@model IEnumerable<string>

@foreach (string tag in Model)
{
	if (!string.IsNullOrWhiteSpace(tag))
	{
	<span class="label label-default">@Html.ActionLink(tag, "Tag", "Pages", new { id = tag }, null)</span>
	}
}